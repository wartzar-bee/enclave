# Code-review template — extra runtime knobs merged by `enclave init` (core keys are generated for you).
# NO inline comments after values.
# A reviewer reports findings; it never edits. The guard already blocks writes/git/deploys, so no
# cloud profile is needed — reviewing is a read-only job. Leave this file minimal; add knobs only if
# your review target needs a specific read-only bridge.
