# Analyst template — extra runtime knobs merged by `enclave init` (core keys are generated for you).
# NO inline comments after values.
# Enable the read-only cloud profile: gcloud/gsutil/bq reads allowed; writes/deploy/IAM/DML blocked.
GUARD_CLOUD_READONLY=1
