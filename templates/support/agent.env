# Support template — extra runtime knobs merged by `enclave init` (core keys are generated for you).
# NO inline comments after values.
# (no extra knobs by default — support is read/draft only; the guard already blocks sends/mutations)
