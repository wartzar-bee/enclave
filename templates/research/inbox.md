# inbox — {AGENT_NAME} (research agent)

<!-- Example first task for the research template. Replace it with your own question;
     delete this block to start fresh (the agent no-ops on an empty inbox). This is a
     safe dry run: the research template REPORTS only — all egress is read-only HTTP
     GET and the guard blocks writes/deploys, so it touches nothing external. -->

- [ ] Research question (answer to `state/chat-reply.md`, following your tick.txt steps —
  fan out the search, fetch the PRIMARY source for every load-bearing claim, adversarially
  verify, and link every figure to its source):

  "What does Anthropic's official documentation say about prompt caching for the Claude
  API — how is a cache write priced relative to a normal input token, and what is the
  default cache lifetime?"

  Cite the primary source (Anthropic's docs) for each figure; label anything you cannot
  corroborate as unverified rather than asserting it.
