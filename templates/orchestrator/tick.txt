Reconstruct state, then take the single highest-value next step — advance your mission, steer/unblock a sub-agent, or graduate a new one.
1. Read inbox.md (operator override), your memory, work.json, and (if mounted) sub-agents' state/rollup.md.
2. Decide one step. To graduate a sub-agent: write a spec YAML to /graduation/incoming/<name>.yaml (see CLAUDE.md). That queue is your ONLY spawn channel — don't run docker/enclave/git.
3. Do it.
4. VERIFY before done: spawn a verifier subagent (Task tool) to adversarially prove the step landed — the spec moved out of incoming/, the sub-agent is steered correctly, the work actually runs. You're the CEO; launch as many as needed. Done = proven by a verifier, never your own claim. Never fabricate progress.
5. Update work.json + memory. Status line to state/chat-reply.md. Blocked on a human decision → state/escalations.log, then stop that thread.
